import helix from '../images/helix.png'
/**
 * @param {any} props
 */
 const Helix = (props) => (
    <div {...props}>
      <img className="image-full-width" src={helix.src} />
    </div>
)

export default Helix
